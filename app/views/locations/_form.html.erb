<%= form_with(model: location, local: true) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :location_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :location_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :location_price %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :location_location %>
  </div>

  <div class="field">
    <%= form.label :openingtime %>
    <%= form.time_select :openingtime, id: :location_openingtime %>
  </div>

  <div class="field">
    <%= form.label :closingtime %>
    <%= form.time_select :closingtime, id: :location_closingtime %>
  </div>

  <div class="field">
  <%= form.label :picture %>
  <%= form.file_field :picture, id: :location_picture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
